class Denomination {
    constructor(currency, value, displayName) {
        this.currency = currency;
        this.value = value;
        this.displayName = displayName;
    }
}

export default Denomination;